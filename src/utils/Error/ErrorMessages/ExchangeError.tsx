import { ErrorTypes } from "../ErrorTypes";

export const ExchangeError = {
    [ErrorTypes.EXCHANGE_ERROR]: {
        666: 'Usted no puede realizar intercambios ya que tiene menos de 4 estrellas',
    }
}